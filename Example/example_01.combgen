/* This simple example shows two string tables: One contains colors, the other contains objects. The expression in the
   last line of the script generates a sentence for each possible combination of colors and objects. */

$color = { "red", "green", "yellow", "blue", "purple", "white", "orange" };
$object = { "balloon", "ball", "dice", "car", "box" };

< "I saw a " . $color . " " . $object . "." >